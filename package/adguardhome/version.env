# renovate: datasource=github-releases depName=AdguardTeam/AdGuardHome versioning=semver-coerced
ADGUARDHOME_VERSION=v0.107.78

# Custom package revision.  A high value makes this package newer than the
# Entware package built from the same upstream version.
PACKAGE_RELEASE=1

PACKAGE_NAME=adguardhome
PACKAGE_ARCH=aarch64-3.10
